# APEX session pointer

The pre-weather-v2 agent and its infrastructure assumptions are historical.
Read `AGENTS.md`, then `README.md`, `ARCHITECTURE.md`, and
`SETTLEMENT_RULES.md`. Do not run `src/kalshi/apex_agent.py`; it is disabled by
default because it contains obsolete multi-strategy execution logic.

Current work belongs on `weather-autonomous-v2`. Paper mode and a $100 bankroll
are the defaults. Never assume an old server, credential, settlement source, or
profit claim is still valid.
